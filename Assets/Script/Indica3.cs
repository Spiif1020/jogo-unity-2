using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Indica3 : MonoBehaviour
{
    public GameObject Form;

    // Start is called before the first frame update
    void Start()
    {
        Destroy(Form, 100f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
